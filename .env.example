# Neural Flow Architect — environment template
# Copy to .env and adjust. Never commit real secrets or neural data paths that leave the device.

NFA_ENV=development
NFA_LOG_LEVEL=INFO

# Local-first defaults
NFA_LOCAL_ONLY=true
NFA_ALLOW_CLOUD_LLM=false
NFA_DATA_DIR=./data

# Signal source: simulator | brainflow | neuralink_stub
NFA_ADAPTER=simulator
NFA_SAMPLE_RATE_HZ=250
NFA_CHANNELS=8

# BrainFlow (optional): pip install -e ".[brainflow]"
# NFA_ADAPTER=brainflow
# NFA_BRAINFLOW_BOARD_ID=-1
# NFA_BRAINFLOW_SERIAL_PORT=
# NFA_BRAINFLOW_FILE=tests/fixtures/synthetic_eeg.csv

# Fail-safe
# NFA_FAILSAFE_STALL_SEC=3
# NFA_FAILSAFE_LOW_QUALITY=0.35
# NFA_FAILSAFE_QUALITY_STREAK=8

# Agent
NFA_AGENT_MODE=rules  # rules | llm_local | llm_cloud (cloud requires explicit consent)
NFA_EXPLAIN_ACTIONS=true
NFA_REQUIRE_CONFIRM_FOR_HIGH_IMPACT=true

# Predictive precursors (research-grade; default off)
NFA_PREDICTIVE_ENABLED=false
NFA_PREDICTIVE_MIN_CONFIDENCE=0.55

# Optional local LLM wording — summaries only, never raw neural samples
NFA_LLM_ENABLED=false
NFA_LLM_BASE_URL=http://127.0.0.1:11434
NFA_LLM_MODEL=llama3.2
NFA_LLM_TIMEOUT_SEC=2.5

# OS notification hooks (companion policy always; OS backend optional)
NFA_OS_NOTIFICATIONS=false
NFA_OS_NOTIFICATIONS_ANNOUNCE=false

# Environment orchestration (disabled by default)
NFA_IOT_ENABLED=false
NFA_IOT_FORCE_DRY_RUN=true
# NFA_HOME_ASSISTANT_URL=
# NFA_HOME_ASSISTANT_TOKEN=

# Optional local active-app context (titles stay on device)
NFA_DETECT_ACTIVE_APP=false

# API
NFA_API_HOST=127.0.0.1
NFA_API_PORT=8741
